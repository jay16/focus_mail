
<div id="<%= dom_id template %>" class="box" >
  <div class="template_info">
    <h3 style="display:inline"><%= template.name %> - <h5 style="display:inline"><%= template.file_name %></h5></h3>
    <ul id="navmenu">
    
    <li><% @info = template.entries && template.entries.present? ? "btn-info" : "" %>
    <a id="<%= dom_id template %>_entries" onclick='if($("#<%= dom_id template %> .entries_info").css("display")=="none") $("#<%= dom_id template %> .entries_info").css("display","block"); else {$("#<%= dom_id template %> .entries_info").css("display","none");}return false;' class="btn btn-mini <%= @info %>">Entrie</a>
    </li>
    
   <li><a class="btn btn-mini" href="#">
      Add Entry  <span class="caret"></span></a>
      <ul>
      <li><%= link_to 'link', new_template_entry_path(template.id), remote: true %></li>
      <li><%= link_to 'image---', new_image_template_entries_path(template.id), remote: true %></li>
      </ul>
    </li>
      
    <li><a class="btn btn-mini" href="#">
      Source <span class="caret"></span></a>
      <ul>
      <li><%= link_to "view", dyna_source_template_path(template.id), :remote => true %></li>
      <li><%= link_to "update", update_source_template_path(template.id), :remote => true %></li></li>
      </ul>
   </li>     
     
     <li>
   <%= link_to 'Edit', edit_template_path(template), :class => 'btn btn-mini', remote: true %>  
     </li>
    
    <li>
      <%= link_to 'Destroy', template_path(template), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger', remote: true %>
     </li>
   </ul>


  </div>

  <div class="entries_info" style="display:none;">
    <% has_entries = template.entries && template.entries.present? %>
    <table class="table table-striped entries" style="display:<%= has_entries ? 'table' : 'none' %>">
      <thead>
        <tr>
          <th>Name</th>
          <th>Content</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <%= render template.entries if has_entries %>
      </tbody>
    </table>
  </div>
  <div class="source hidden">
  </div>
   <div class="template_form">
  </div>
</div>
