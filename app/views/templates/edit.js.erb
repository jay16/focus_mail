$("#templates_form").html("<%= j render "form" %>");
$("#fjzip").click(function(){
	if($(this).text() == "zip上传")
	{
		$("#htmlsource").addClass("hidden");
		$("#zipfile").removeClass("hidden");
		$(this).text("内容填写");
	}
	else
	{
		$("#htmlsource").removeClass("hidden");
		$("#zipfile").removeClass("hidden");
		$(this).text("zip上传");
	}
});
  $("#template_add_edit").html("修改邮件模板");
  
  hideLoading();

