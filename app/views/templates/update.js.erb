$("#<%= dom_id @template %>").replaceWith("<%= j render @template %>");
<%  @template = Template.new %>
$("#form > form").replaceWith("<%= j render "form" %>");
$("#new_template")[0].reset();

$("#template_add_edit").html("新建邮件模板")
$("#template_form a").removeClass("disabled");
hideLoading();
