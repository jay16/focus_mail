<%- model_class = List.new.class -%>
<h1 style="display:inline"><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
  <div style="float:right">
    <%= link_to 'Add a List', new_list_path, :class => 'btn btn-primary', remote: true %>
  </div>
<table class="table table-striped" id="lists">
  <thead>
    <tr>
      <th>Name</th>
      <th>Number</th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @lists.each do |list| %>
      <%= render list %>
    <% end %>
  </tbody>
</table>

<hr />

<div id="form">
<div id="addmember"><div>
</div>
