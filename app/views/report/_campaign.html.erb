<div id="report_title" style="margin-bottom: 20px">
		<h1 style="display:inline"> <%= @campaign_name %> </h1>
		<div style="float:right">
				  <a onclick="$('#form').fadeOut().html('');" class='btn btn-primary'>back</a>
				</div>
</div>
size:<%= @responsed.size %><br>

<%= high_chart("chart_id1", @h) %>

<table id="report_table" class="table table-striped">
  <thead>
    <tr>
      <th>Template</th>
      <th>Create_at</th>
      <th>Tracks</th>
      <th>Clicks</th>
    </tr>
  </thead>
  <tbody>
    <% @rule.first(20).each do |rule| %>
      <tr>
        <td><%= @campaign_name %></td>
        <td><%= rule[0] %></td>
        <td><%= rule[1] %></td>
        <td><%= rule[2] %></td>
      </tr>
    <% end %>
  </tbody>
</table>



