$("#<%= dom_id @entry %>").remove();
<% if @template.entries.empty? %>
    $("#<%= dom_id @template %> .entries").css('display', 'none');
    $("#<%= dom_id @template %> #<%= dom_id @template %>_entries").each(function(){$(this).removeClass("btn-info");});
<% end %>
