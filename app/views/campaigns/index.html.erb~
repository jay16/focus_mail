<%- model_class = Campaign.new.class -%>
<div id="templates_title" style="margin-bottom: 20px">
<h1  style="display:inline"><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
  <div style="float:right">
    <%= link_to 'Add a Campaign', new_campaign_path, :class => 'btn', remote: true %>
  </div>
</div>

<table class="table table-striped" id="campaigns">
  <thead>
    <tr>
      <th>Name</th>
      <th>From name</th>
      <th>From email</th>
      <th>Subject</th>
      <th>Template</th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @campaigns.each do |campaign| %>
      <%= render campaign %>
    <% end %>
  </tbody>
</table>

<hr />

<div id="form">
</div>

